apiVersion: v1
kind: Service
metadata:
  name: review-service
  labels:
    app.kubernetes.io/name: review-service
    {{- include "myreadings.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: review-service
  ports:
    - name: http
      port: {{ .Values.review.port }}
      targetPort: {{ .Values.review.port }}
      protocol: TCP
